name: 'healthcare_claims'
version: '1.0.0'
config-version: 2

profile: 'healthcare_claims'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
seed-paths: ["seeds"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  healthcare_claims:
    bronze:
      +materialized: table
      +schema: BRONZE
    silver:
      +materialized: view
      +schema: SILVER
    gold:
      +materialized: table
      +schema: GOLD
